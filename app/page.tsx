'use client';

import { AnimatePresence, motion, useInView, useReducedMotion } from 'framer-motion';
import { ArrowRight, BarChart3, BrainCircuit, CheckCircle2, Database, Layers3, Pause, Play, ShieldCheck, Sparkles, UsersRound } from 'lucide-react';
import { useRef, useState } from 'react';
import { Hero } from '@/components/home/Hero';
import { Button } from '@/components/ui/Button';
import { GlassCard } from '@/components/ui/GlassCard';

const challenges = [
  { label: 'SCATTERED SYSTEMS', title: 'Information lives everywhere.', description: 'Important information sits across disconnected tools, files, and workflows, making the complete picture difficult to see.', icon: Database },
  { label: 'SLOW PREPARATION', title: 'Preparation slows momentum.', description: 'Teams lose time finding, cleaning, and checking data before meaningful work can begin.', icon: Layers3 },
  { label: 'UNCERTAIN AI FOUNDATIONS', title: 'AI needs something dependable.', description: 'AI initiatives struggle when the underlying data is incomplete, inconsistent, or difficult to trust.', icon: BrainCircuit },
];

const journey = [
  ['01', 'Connect', 'Bring the sources that matter together in one clear workflow.'],
  ['02', 'Shape', 'Organize information so teams can work with it confidently.'],
  ['03', 'Trust', 'Build quality and governance into the way data moves.'],
  ['04', 'Activate', 'Prepare intelligence for AI, analytics, and daily decisions.'],
] as const;

const aiOutcomes = [
  ['Search and knowledge assistants', 'Give teams a dependable foundation for finding answers across enterprise information.', Sparkles],
  ['Forecasting and prediction', 'Prepare consistent data that helps planners see what may happen next.', BarChart3],
  ['Customer intelligence', 'Connect the signals needed to understand customers and improve every interaction.', UsersRound],
  ['Automated decisions', 'Turn trusted information into repeatable actions across operational workflows.', CheckCircle2],
] as const;

const audiences = [
  ['Data engineering teams', 'Spend less time untangling preparation work and more time building the systems your business needs.'],
  ['AI and analytics teams', 'Start with cleaner, better-prepared data so experiments can become useful products.'],
  ['Enterprise decision makers', 'Create a dependable foundation for confident reporting, planning, and intelligent automation.'],
] as const;

const testimonials = [
  { role: 'Illustrative customer story · Data engineering team', challenge: 'Preparation work was slowing every new data initiative.', transformation: 'A clearer workflow helped the team spend more time building and less time untangling inputs.', quote: 'We can finally give our teams a dependable starting point instead of another data clean-up project.' },
  { role: 'Illustrative customer story · AI and analytics team', challenge: 'Promising experiments were difficult to turn into repeatable outcomes.', transformation: 'Better-prepared information created a stronger foundation for search, forecasting, and experimentation.', quote: 'The difference is not just cleaner data. It is more confidence in what our AI work can deliver.' },
  { role: 'Illustrative customer story · Enterprise operations team', challenge: 'Important decisions depended on inconsistent views of the business.', transformation: 'Shared, trusted workflows made reporting and operational decisions easier to align.', quote: 'When everyone can work from the same trusted foundation, decisions move with much less friction.' },
] as const;

export default function HomePage() {
  return <div><Hero /><Challenges /><TransformationJourney /><AIOutcomes /><Testimonials /><OutcomeStatements /><AudienceSection /><FinalCTA /></div>;
}

function Challenges() {
  const reducedMotion = useReducedMotion();
  const [selected, setSelected] = useState(0);
  return <section aria-labelledby="challenge-title" className="mx-auto max-w-7xl px-5 py-20 lg:px-8 lg:py-28"><div className="grid items-start gap-12 lg:grid-cols-[0.8fr_1.2fr] lg:gap-20"><Reveal><div className="relative min-h-[500px] overflow-hidden rounded-[2rem] border border-brand-100 bg-brand-50/55 p-7 shadow-panel sm:min-h-[560px] sm:p-10"><div className="absolute inset-0 bg-[url('https://images.unsplash.com/photo-1518770660439-4636190af475?auto=format&fit=crop&w=1200&q=80')] bg-cover bg-center opacity-40 mix-blend-multiply" aria-hidden="true" /><div className="absolute inset-0 bg-gradient-to-b from-brand-50/45 via-brand-50/45 to-brand-100/90" aria-hidden="true" /><div className="absolute right-0 top-0 h-48 w-48 rounded-full bg-brand-200/35 blur-3xl" aria-hidden="true" /><div className="relative z-10 flex h-full min-h-[440px] flex-col"><p className="relative eyebrow">WHY TEAMS LOOK FOR A BETTER WAY</p><h2 id="challenge-title" className="relative mt-5 text-3xl font-semibold leading-tight tracking-tight text-slate-800 sm:text-4xl">Complexity is the cost of disconnected data.</h2><p className="relative mt-5 max-w-md text-base leading-7 text-slate-600">Your teams should be building what comes next — not repeatedly preparing the same information.</p><div className="relative mt-auto h-28 overflow-hidden rounded-2xl border border-brand-200/80 bg-white/35 backdrop-blur-sm" aria-hidden="true"><div className="absolute left-[-12%] top-8 h-px w-[125%] rotate-[-12deg] bg-brand-500/65" /><div className="absolute left-[-10%] top-14 h-px w-[120%] rotate-[9deg] bg-brand-400/65" /><div className="absolute left-[12%] top-5 h-2 w-2 rounded-full bg-brand-600 shadow-[0_0_16px_rgba(154,101,0,0.45)]" /><div className="absolute right-[18%] top-16 h-3 w-3 rounded-full bg-brand-500 shadow-[0_0_18px_rgba(183,121,0,0.5)]" /><div className="absolute left-[48%] top-10 h-2 w-2 rounded-full bg-brand-400" /></div></div></div></Reveal><div className="space-y-3">{challenges.map(({ label, title, description, icon: Icon }, index) => { const active = selected === index; return <Reveal key={title} delay={index * 0.08}><motion.button type="button" onClick={() => setSelected(index)} aria-pressed={active} whileHover={reducedMotion ? undefined : { x: 7 }} whileTap={reducedMotion ? undefined : { scale: 0.99 }} className={`group relative block w-full overflow-hidden rounded-2xl border p-5 text-left transition focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-brand-400 focus-visible:ring-offset-2 focus-visible:ring-offset-[#fffdf2] sm:p-6 ${active ? 'border-brand-400 bg-white/90 shadow-brand' : 'border-brand-100 bg-white/60 opacity-75 hover:border-brand-300/70 hover:opacity-100'}`}><motion.span initial={false} animate={{ scaleX: active ? 1 : 0.12 }} transition={{ duration: reducedMotion ? 0 : 0.35 }} className="absolute inset-y-0 left-0 w-1 origin-left bg-brand-400" aria-hidden="true" /><div className="flex items-start gap-4"><span className={`grid h-11 w-11 shrink-0 place-items-center rounded-xl transition ${active ? 'bg-brand-400 text-slate-950' : 'bg-brand-100 text-brand-600 group-hover:bg-brand-200'}`}><Icon size={21} /></span><span className="min-w-0"><span className="block font-mono text-[0.62rem] font-semibold tracking-[0.16em] text-brand-600">{label}</span><span className="mt-2 block text-xl font-semibold text-slate-800">{title}</span><motion.span initial={false} animate={{ opacity: active ? 1 : 0.72, height: active ? 'auto' : '1.5rem' }} className="mt-2 block overflow-hidden text-sm leading-6 text-slate-500">{description}</motion.span></span></div><span className={`absolute right-5 top-6 h-2 w-2 rounded-full transition ${active ? 'bg-brand-400 shadow-[0_0_12px_rgba(183,121,0,0.5)]' : 'bg-brand-200 group-hover:bg-brand-400'}`} aria-hidden="true" /></motion.button></Reveal>; })}</div></div></section>;
}

function LegacyTransformationJourney() {
  const reducedMotion = useReducedMotion();
  const [selected, setSelected] = useState(0);
  const [previewed, setPreviewed] = useState<number | null>(null);
  const activeIndex = previewed ?? selected;
  const stageIcons = [Database, Layers3, ShieldCheck, Sparkles] as const;
  const activeStage = journey[activeIndex];

  return <section id="how-it-works" aria-labelledby="journey-title" className="overflow-hidden px-5 py-20 lg:px-8 lg:py-28"><div className="mx-auto max-w-7xl"><div className="grid items-end gap-10 lg:grid-cols-[0.7fr_1.3fr] lg:gap-20"><Reveal><div><p className="eyebrow">A CLEARER PATH FORWARD</p><h2 id="journey-title" className="mt-4 max-w-md text-4xl font-semibold leading-tight tracking-tight text-white sm:text-5xl">A clearer way forward.</h2><p className="mt-5 max-w-md text-base leading-7 text-slate-500">Move from scattered information to useful intelligence through a journey your teams can understand and build on.</p><div className="relative mt-10 hidden h-28 overflow-hidden rounded-3xl border border-brand-100 bg-brand-50/45 lg:block" aria-hidden="true"><div className="absolute -left-8 top-16 h-px w-[120%] rotate-[-12deg] bg-brand-400/45" /><div className="absolute -left-4 top-10 h-px w-[115%] rotate-[16deg] bg-brand-300/55" /><div className="absolute left-[23%] top-10 h-2 w-2 rounded-full bg-brand-400 shadow-[0_0_16px_rgba(183,121,0,0.45)]" /><div className="absolute right-[25%] top-16 h-3 w-3 rounded-full bg-brand-500 shadow-[0_0_20px_rgba(183,121,0,0.4)]" /></div></div></Reveal><Reveal delay={0.1}><div className="relative rounded-[2rem] border border-brand-100 bg-brand-50/55 p-5 shadow-panel sm:p-7"><div className="pointer-events-none absolute inset-0 rounded-[2rem] bg-[radial-gradient(circle_at_90%_10%,rgba(234,179,8,0.18),transparent_34%)]" aria-hidden="true" /><div className="relative"><div className="mb-5 flex items-center justify-between"><p className="font-mono text-[0.65rem] font-semibold uppercase tracking-[0.2em] text-brand-600">THE JOURNEY</p><span className="text-xs text-slate-500">{String(activeIndex + 1).padStart(2, '0')} / 04</span></div><div className="relative overflow-x-auto pb-2"><div className="absolute left-8 right-8 top-8 hidden h-px bg-brand-200 md:block" aria-hidden="true" /><motion.div initial={false} animate={{ width: `${(activeIndex / (journey.length - 1)) * 100}%` }} transition={{ duration: reducedMotion ? 0 : 0.45, ease: 'easeOut' }} className="absolute left-8 top-8 hidden h-0.5 max-w-[calc(100%-4rem)] bg-brand-400 md:block" aria-hidden="true" /><div className="relative grid min-w-[620px] grid-cols-4 gap-3 md:min-w-0">{journey.map(([number, title], index) => { const Icon = stageIcons[index]; const active = activeIndex === index; return <motion.button key={number} type="button" onClick={() => setSelected(index)} onMouseEnter={() => setPreviewed(index)} onMouseLeave={() => setPreviewed(null)} onFocus={() => setPreviewed(index)} onBlur={() => setPreviewed(null)} aria-label={`${number} ${title}`} aria-pressed={selected === index} whileHover={reducedMotion ? undefined : { y: -4 }} whileTap={reducedMotion ? undefined : { scale: 0.98 }} className={`group relative rounded-2xl border p-3 text-left transition focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-brand-400 focus-visible:ring-offset-2 focus-visible:ring-offset-[#fffdf2] ${active ? 'border-brand-400 bg-white/95 shadow-brand' : 'border-brand-100 bg-white/55 hover:border-brand-300'}`}><span className={`relative z-10 grid h-10 w-10 place-items-center rounded-xl border transition ${active ? 'border-brand-400 bg-brand-400 text-slate-950' : 'border-brand-200 bg-brand-50 text-brand-600 group-hover:border-brand-400 group-hover:text-brand-700'}`}><Icon size={18} /></span><span className="mt-4 block font-mono text-xs font-semibold text-brand-600">{number}</span><span className="mt-1 block text-base font-semibold text-slate-800">{title}</span></motion.button>; })}</div></div><AnimatePresence mode="wait"><motion.div key={activeIndex} initial={{ opacity: 0, y: reducedMotion ? 0 : 8 }} animate={{ opacity: 1, y: 0 }} exit={{ opacity: 0, y: reducedMotion ? 0 : -8 }} transition={{ duration: reducedMotion ? 0 : 0.25 }} className="mt-5 border-t border-brand-100 pt-5"><p className="text-lg font-semibold text-slate-800">{activeStage[1]} information into momentum.</p><p className="mt-2 max-w-xl text-sm leading-6 text-slate-500">{activeStage[2]}</p></motion.div></AnimatePresence></div></div></Reveal></div></div></section>;
}

function TransformationJourney() {
  const reducedMotion = useReducedMotion();
  const [selected, setSelected] = useState(0);
  const [previewed, setPreviewed] = useState<number | null>(null);
  const activeIndex = previewed ?? selected;
  const stageIcons = [Database, Layers3, ShieldCheck, Sparkles] as const;
  const activeStage = journey[activeIndex];

  return <section id="how-it-works" aria-labelledby="journey-title" className="overflow-hidden px-5 py-20 lg:px-8 lg:py-28"><div className="mx-auto max-w-7xl"><div className="grid items-end gap-10 lg:grid-cols-[0.7fr_1.3fr] lg:gap-20"><Reveal><div><p className="eyebrow">A CLEARER PATH FORWARD</p><h2 id="journey-title" className="mt-4 max-w-md text-4xl font-semibold leading-tight tracking-tight text-white sm:text-5xl">A clearer way forward.</h2><p className="mt-5 max-w-md text-base leading-7 text-slate-500">Move from scattered information to useful intelligence through a journey your teams can understand and build on.</p><div className="mt-8 max-w-md border-l-2 border-brand-300 pl-5"><p className="text-sm font-semibold text-slate-700">One connected workflow.</p><p className="mt-2 text-sm leading-6 text-slate-500">Each step builds on the one before it, giving teams a clearer path from raw information to useful action.</p></div></div></Reveal><Reveal delay={0.1}><div className="relative rounded-[2rem] border border-brand-100 bg-brand-50/55 p-5 shadow-panel sm:p-7"><div className="pointer-events-none absolute inset-0 rounded-[2rem] bg-[radial-gradient(circle_at_90%_10%,rgba(234,179,8,0.18),transparent_34%)]" aria-hidden="true" /><div className="relative"><div className="mb-5 flex items-center justify-between"><p className="font-mono text-[0.65rem] font-semibold uppercase tracking-[0.2em] text-brand-600">THE JOURNEY</p><span className="text-xs text-slate-500">{String(activeIndex + 1).padStart(2, '0')} / 04</span></div><div className="relative overflow-x-auto pb-2 pt-2"><div className="absolute left-8 right-8 top-10 hidden h-px bg-brand-200 md:block" aria-hidden="true" /><motion.div initial={false} animate={{ width: `${(activeIndex / (journey.length - 1)) * 100}%` }} transition={{ duration: reducedMotion ? 0 : 0.45, ease: 'easeOut' }} className="absolute left-8 top-10 hidden h-0.5 max-w-[calc(100%-4rem)] bg-brand-400 md:block" aria-hidden="true" /><div className="relative grid min-w-[620px] grid-cols-4 gap-3 md:min-w-0">{journey.map(([number, title], index) => { const Icon = stageIcons[index]; const active = activeIndex === index; return <motion.button key={number} type="button" onClick={() => setSelected(index)} onMouseEnter={() => setPreviewed(index)} onMouseLeave={() => setPreviewed(null)} onFocus={() => setPreviewed(index)} onBlur={() => setPreviewed(null)} aria-label={`${number} ${title}`} aria-pressed={selected === index} whileTap={reducedMotion ? undefined : { scale: 0.98 }} className={`group relative rounded-2xl border p-3 text-left transition focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-brand-400 focus-visible:ring-offset-2 focus-visible:ring-offset-[#fffdf2] ${active ? 'border-brand-400 bg-white/95 shadow-brand' : 'border-brand-100 bg-white/55 hover:border-brand-300 hover:shadow-panel'}`}><span className={`relative z-10 grid h-10 w-10 place-items-center rounded-xl border transition ${active ? 'border-brand-400 bg-brand-400 text-slate-950' : 'border-brand-200 bg-brand-50 text-brand-600 group-hover:border-brand-400 group-hover:text-brand-700'}`}><Icon size={18} /></span><span className="mt-4 block font-mono text-xs font-semibold text-brand-600">{number}</span><span className="mt-1 block text-base font-semibold text-slate-800">{title}</span></motion.button>; })}</div></div><AnimatePresence mode="wait"><motion.div key={activeIndex} initial={{ opacity: 0, y: reducedMotion ? 0 : 8 }} animate={{ opacity: 1, y: 0 }} exit={{ opacity: 0, y: reducedMotion ? 0 : -8 }} transition={{ duration: reducedMotion ? 0 : 0.25 }} className="mt-5 border-t border-brand-100 pt-5"><p className="text-lg font-semibold text-slate-800">{activeStage[1]}</p><p className="mt-2 max-w-xl text-sm leading-6 text-slate-500">{activeStage[2]}</p></motion.div></AnimatePresence></div></div></Reveal></div></div></section>;
}

function AIOutcomes() {
  return <section className="mx-auto max-w-7xl px-5 py-20 lg:px-8 lg:py-28"><div className="grid items-center gap-10 lg:grid-cols-[0.8fr_1.2fr]"><Reveal><div><p className="eyebrow">AI-READY BY DESIGN</p><h2 className="mt-4 text-3xl font-semibold tracking-tight text-white sm:text-4xl">Better AI starts with data your teams can trust.</h2><p className="mt-5 max-w-xl text-base leading-7 text-slate-500">DataSculpt prepares the foundation behind useful enterprise AI, so your teams can move from interesting experiments to dependable outcomes.</p><Button href="/architecture/" variant="ghost" className="mt-7">See the system approach <ArrowRight size={15} /></Button></div></Reveal><div className="grid gap-4 sm:grid-cols-2">{aiOutcomes.map(([title, description, Icon], index) => <Reveal key={title} delay={index * 0.06}><motion.div whileHover={{ y: -4 }} className="h-full rounded-2xl border border-brand-100 bg-brand-50/70 p-5 transition hover:border-brand-300/70"><Icon size={20} className="text-brand-500" /><h3 className="mt-5 text-base font-semibold text-slate-800">{title}</h3><p className="mt-2 text-sm leading-6 text-slate-500">{description}</p></motion.div></Reveal>)}</div></div></section>;
}

function Testimonials() {
  const reduceMotion = useReducedMotion();
  const sectionRef = useRef<HTMLElement>(null);
  const visible = useInView(sectionRef, { once: false, amount: 0.25 });
  const [selected, setSelected] = useState(0);
  const [paused, setPaused] = useState(Boolean(reduceMotion));
  const story = testimonials[selected];
  const selectStory = (index: number) => { setSelected(index); setPaused(true); };
  return <section ref={sectionRef} aria-labelledby="customer-stories-title" className="mx-auto max-w-7xl px-5 py-20 lg:px-8 lg:py-28"><div className="mb-10 max-w-2xl"><Reveal><p className="eyebrow">THE OUTCOME PEOPLE REMEMBER</p><h2 id="customer-stories-title" className="mt-4 text-3xl font-semibold tracking-tight text-white sm:text-4xl">Trusted data creates confident action.</h2><p className="mt-4 text-base leading-7 text-slate-500">Representative customer stories showing the kinds of change DataSculpt is designed to support.</p></Reveal></div><div className="relative min-h-[650px] overflow-hidden rounded-[2rem] border border-brand-100 bg-brand-50/55 p-5 sm:min-h-[500px] sm:p-10 lg:min-h-[460px]"><motion.div animate={reduceMotion || paused || !visible ? undefined : { rotate: 360 }} transition={reduceMotion || paused || !visible ? undefined : { duration: 32, repeat: Infinity, ease: 'linear' }} className="absolute left-1/2 top-1/2 hidden h-[420px] w-[420px] -translate-x-1/2 -translate-y-1/2 rounded-full border border-dashed border-brand-300/55 md:block" aria-hidden="true" /><div className="relative z-10 flex min-h-[610px] flex-col items-center justify-center sm:min-h-[460px]"><AnimatePresence mode="wait"><motion.div key={selected} initial={{ opacity: 0, scale: reduceMotion ? 1 : 0.96 }} animate={{ opacity: 1, scale: 1 }} exit={{ opacity: 0, scale: reduceMotion ? 1 : 1.02 }} transition={{ duration: reduceMotion ? 0 : 0.3 }} className="order-1 w-full max-w-md rounded-3xl border border-brand-300/80 bg-white/90 p-6 text-center shadow-brand backdrop-blur-xl sm:p-8" aria-live="polite"><span className="mx-auto grid h-14 w-14 place-items-center rounded-2xl bg-brand-400 text-slate-950"><BrainCircuit size={28} /></span><p className="mt-5 font-mono text-[0.62rem] uppercase tracking-[0.18em] text-brand-600">{story.role}</p><h3 className="mt-4 text-2xl font-semibold text-slate-800">A stronger foundation for what comes next.</h3><p className="mt-4 text-sm leading-6 text-slate-500">{story.transformation}</p><blockquote className="mt-5 border-t border-brand-100 pt-5 text-base font-medium leading-7 text-slate-700">“{story.quote}”</blockquote></motion.div></AnimatePresence><div className="order-2 mt-7 flex max-w-3xl flex-wrap justify-center gap-3 md:absolute md:inset-0 md:mt-0">{testimonials.map((item, index) => <motion.button key={item.role} type="button" onClick={() => selectStory(index)} aria-pressed={selected === index} aria-label={`Show ${item.role}`} whileHover={reduceMotion ? undefined : { scale: 1.04 }} className={`rounded-2xl border px-4 py-3 text-left text-sm transition focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-brand-400 md:absolute md:w-64 ${index === 0 ? 'md:left-2 md:top-10' : index === 1 ? 'md:right-2 md:top-1/2 md:-translate-y-1/2' : 'md:bottom-10 md:left-1/2 md:-translate-x-1/2'} ${selected === index ? 'border-brand-400 bg-brand-100 text-brand-700 shadow-brand' : 'border-brand-100 bg-white/80 text-slate-600 hover:border-brand-300 hover:bg-white'}`}><span className="block font-semibold">{item.challenge}</span><span className="mt-1 block text-xs text-slate-500">Select story {index + 1}</span></motion.button>)}</div><button type="button" onClick={() => setPaused((value) => !value)} className="order-3 mt-6 inline-flex items-center gap-2 rounded-full border border-brand-200 bg-white/75 px-3 py-2 text-xs font-semibold text-brand-700 transition hover:border-brand-400 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-brand-400" aria-label={paused ? 'Resume testimonial animation' : 'Pause testimonial animation'}>{paused ? <Play size={13} /> : <Pause size={13} />}{paused ? 'Resume motion' : 'Pause motion'}</button></div></div></section>;
}

function OutcomeStatements() {
  const outcomes = ['Move from raw data to usable insight faster', 'Build confidence into every data workflow', 'Give AI teams cleaner, better-prepared foundations'];
  return <section className="border-y border-brand-100 bg-brand-50/65"><div className="mx-auto grid max-w-7xl gap-0 px-5 py-3 lg:grid-cols-3 lg:px-8">{outcomes.map((outcome, index) => <div key={outcome} className="flex items-center gap-3 border-brand-100 px-4 py-5 text-sm font-medium text-slate-700 lg:border-r lg:py-4 lg:last:border-r-0"><span className="h-2 w-2 shrink-0 rounded-full bg-brand-400" />{outcome}</div>)}</div></section>;
}

function AudienceSection() {
  return <SectionShell eyebrow="MADE FOR THE PEOPLE MOVING DATA FORWARD" title="One foundation. Every team moves better." description="DataSculpt connects the needs of technical builders and business leaders around one trusted data foundation."><div className="grid gap-5 md:grid-cols-3">{audiences.map(([title, description], index) => <Reveal key={title} delay={index * 0.08}><GlassCard className="h-full border-brand-100 bg-white/75 p-6"><ShieldCheck size={20} className="text-brand-500" /><h3 className="mt-5 text-xl font-semibold text-slate-800">{title}</h3><p className="mt-3 text-sm leading-6 text-slate-500">{description}</p></GlassCard></Reveal>)}</div></SectionShell>;
}

function FinalCTA() {
  return <section className="mx-auto max-w-7xl px-5 pb-24 pt-8 lg:px-8 lg:pb-32"><motion.div initial={{ opacity: 0, y: 15 }} whileInView={{ opacity: 1, y: 0 }} viewport={{ once: true }} className="rounded-3xl border border-brand-300/70 bg-gradient-to-r from-brand-50 via-brand-100/75 to-brand-50 p-8 text-center shadow-brand sm:p-12"><p className="eyebrow">READY FOR THE NEXT STEP?</p><h2 className="mx-auto mt-4 max-w-2xl text-3xl font-semibold tracking-tight text-slate-800 sm:text-4xl">Make your data foundation ready for what&apos;s next.</h2><p className="mx-auto mt-4 max-w-xl text-sm leading-6 text-slate-600">See how DataSculpt can help your teams create more value from the data they already have.</p><div className="mt-8 flex flex-col justify-center gap-3 sm:flex-row"><Button href="/products/">Explore the platform <ArrowRight size={16} /></Button><Button href="/contact/" variant="ghost">Explore your use case</Button></div></motion.div></section>;
}

function SectionShell({ id, eyebrow, title, description, children }: { id?: string; eyebrow: string; title: string; description: string; children: React.ReactNode }) {
  return <section id={id} className="mx-auto max-w-7xl px-5 py-20 lg:px-8 lg:py-28"><div className="mb-10 max-w-2xl"><Reveal><p className="eyebrow">{eyebrow}</p><h2 className="mt-4 text-3xl font-semibold tracking-tight text-white sm:text-4xl">{title}</h2><p className="mt-4 text-base leading-7 text-slate-500">{description}</p></Reveal></div>{children}</section>;
}

function Reveal({ children, delay = 0 }: { children: React.ReactNode; delay?: number }) {
  const reduceMotion = useReducedMotion();
  return <motion.div initial={{ opacity: 0, y: reduceMotion ? 0 : 16 }} whileInView={{ opacity: 1, y: 0 }} viewport={{ once: true, amount: 0.15 }} transition={{ delay: reduceMotion ? 0 : delay, duration: reduceMotion ? 0 : 0.5 }}>{children}</motion.div>;
}
