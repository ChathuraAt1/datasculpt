'use client';

import { AnimatePresence, motion, useInView, useReducedMotion } from 'framer-motion';
import { BrainCircuit, Pause, Play } from 'lucide-react';
import { useRef, useState } from 'react';
import { testimonials } from './homeData';
import { Reveal } from './Reveal';

export function Testimonials() {
  const reduceMotion = useReducedMotion(); const sectionRef = useRef<HTMLElement>(null); const visible = useInView(sectionRef, { once: false, amount: 0.25 }); const [selected, setSelected] = useState(0); const [paused, setPaused] = useState(Boolean(reduceMotion)); const story = testimonials[selected];
  return <section ref={sectionRef} aria-labelledby="customer-stories-title" className="mx-auto max-w-7xl px-5 py-20 lg:px-8 lg:py-28"><div className="mb-10 max-w-2xl"><Reveal><p className="eyebrow">THE OUTCOME PEOPLE REMEMBER</p><h2 id="customer-stories-title" className="mt-4 text-3xl font-semibold tracking-tight text-white sm:text-4xl">Trusted data creates confident action.</h2><p className="mt-4 text-base leading-7 text-slate-500">Representative customer stories showing the kinds of change DataSculpt is designed to support.</p></Reveal></div><div className="relative min-h-[650px] overflow-hidden rounded-[2rem] border border-brand-100 bg-brand-50/55 p-5 sm:min-h-[500px] sm:p-10 lg:min-h-[460px]"><motion.div animate={reduceMotion || paused || !visible ? undefined : { rotate: 360 }} transition={reduceMotion || paused || !visible ? undefined : { duration: 32, repeat: Infinity, ease: 'linear' }} className="absolute left-1/2 top-1/2 hidden h-[420px] w-[420px] -translate-x-1/2 -translate-y-1/2 rounded-full border border-dashed border-brand-300/55 md:block" aria-hidden="true" /><div className="relative z-10 flex min-h-[610px] flex-col items-center justify-center sm:min-h-[460px]"><AnimatePresence mode="wait"><motion.div key={selected} initial={{ opacity: 0 }} animate={{ opacity: 1 }} exit={{ opacity: 0 }} className="order-1 w-full max-w-md rounded-3xl border border-brand-300/80 bg-white/90 p-6 text-center shadow-brand sm:p-8" aria-live="polite"><span className="mx-auto grid h-14 w-14 place-items-center rounded-2xl bg-brand-400 text-slate-950"><BrainCircuit size={28} /></span><p className="mt-5 font-mono text-[0.62rem] uppercase tracking-[0.18em] text-brand-600">{story.role}</p><h3 className="mt-4 text-2xl font-semibold text-slate-800">A stronger foundation for what comes next.</h3><p className="mt-4 text-sm leading-6 text-slate-500">{story.transformation}</p><blockquote className="mt-5 border-t border-brand-100 pt-5 text-base font-medium leading-7 text-slate-700">“{story.quote}”</blockquote></motion.div></AnimatePresence><div className="order-2 mt-7 flex max-w-3xl flex-wrap justify-center gap-3 md:absolute md:inset-0 md:mt-0">{testimonials.map((item, index) => <button key={item.role} type="button" onClick={() => { setSelected(index); setPaused(true); }} aria-pressed={selected === index} className={`rounded-2xl border px-4 py-3 text-left text-sm transition focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-brand-400 md:absolute md:w-64 ${index === 0 ? 'md:left-2 md:top-10' : index === 1 ? 'md:right-2 md:top-1/2 md:-translate-y-1/2' : 'md:bottom-10 md:left-1/2 md:-translate-x-1/2'} ${selected === index ? 'border-brand-400 bg-brand-100 text-brand-700 shadow-brand' : 'border-brand-100 bg-white/80 text-slate-600 hover:border-brand-300'}`}><span className="block font-semibold">{item.challenge}</span><span className="mt-1 block text-xs text-slate-500">Select story {index + 1}</span></button>)}</div><button type="button" onClick={() => setPaused((value) => !value)} className="order-3 mt-6 inline-flex items-center gap-2 rounded-full border border-brand-200 bg-white/75 px-3 py-2 text-xs font-semibold text-brand-700 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-brand-400">{paused ? <Play size={13} /> : <Pause size={13} />}{paused ? 'Resume motion' : 'Pause motion'}</button></div></div></section>;
}
